/**
 * Values the build puts into the code.
 *
 * The version and the short commit are stamped in at build time so a set of
 * timings can be told apart from a set measured before a change. See the
 * `define` block in vite.config.ts.
 */
declare const __APP_VERSION__: string;
